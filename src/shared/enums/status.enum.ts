export enum Status {

    // Use for donate and receive 
    PENDING = 'PENDING',
    APPROVED = 'APPROVED',
    REJECTED = 'REJECTED',
    CANCELLED = 'CANCELLED',
    COMPLETED = 'COMPLETED',

    // Use for storage
    STORAGE = 'STORAGE',
    EXPORTED = 'EXPORTED'
}